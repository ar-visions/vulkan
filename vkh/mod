dep(mx:mx mx:async vulkan:vk @Vulkan) # on Windows it does not get the public Vulkan inside of vulkan/vk

if(MSVC)
    cflags(/wd4838 /wd4061 /wd4062 /wd4473 /wd4474 /wd4475)
else()
    cflags(+-Wno-c++11-narrowing +-Wno-switch +-Wno-format)
endif()

file(TO_CMAKE_PATH "$ENV{VULKAN_SDK}/Include" VULKAN_INCLUDE)
includes(${VULKAN_INCLUDE})

defines(VKH_USE_VMA)

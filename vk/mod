dep(mx:mx mx:image mx:watch glm:glm_static glfw:glfw3)

if(mac)
    dep(IOSurface QuartzCore Metal AppKit CoreGraphics CoreFoundation Cocoa IOKit moltenvk:MoltenVK)
elseif(linux)
    dep(X11 Vulkan)
else()
    dep(Vulkan) # This is finding the CMake package for Vulkan.
endif()
